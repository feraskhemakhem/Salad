<!DOCTYPE html >
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no" />
    <meta http-equiv="content-type" content="text/html; charset=UTF-8"/>
    <title>Samaritan</title>
    <style>

      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
        align-items: center;
        align-content: center;
        align-self: center;
        color: #f6f7fb;
        font-size: 1rem;
        background-color: #052542;

        font-family: 'Century Gothic';

        -webkit-animation: fadein 2s; /* Safari, Chrome and Opera > 12.1 */
         -moz-animation: fadein 2s; /* Firefox < 16 */
          -ms-animation: fadein 2s; /* Internet Explorer */
           -o-animation: fadein 2s; /* Opera < 12.1 */
              animation: fadein 2s;
  }

  #main{
    position: relative;
    top: 50%;
    transform: translateY(-50%);

        align-items: center;
        align-content: center;

  }

  #potato {
    position: inherit;
    align-items: center;
    align-content: center;
    padding-bottom: 2rem;
    font-family: inherit;
    font-size: 40pt;
    color: #f6f7fb;
    font-weight: 200;
  }

  #purrtato {
  font-family: inherit;
   width: 18%;
   padding-top: 25px;
   font-size: 1rem;
   padding-bottom: 25px;
    position: inherit;
    right: 4%;
    color: #f6f7fb;
    background-color: #7a9ec7;
    border-radius: 6px;
    border: none;
  }

  #purrtato button{
    width: 15rem;
    height: 4rem;

    align-items: center;
    align-content: center;

    margin-top: 2rem;
    margin-bottom: 2rem;

    display: block;
    border: none;

    font: inherit;
    color: #f6f7fb;

  }

  #purrtato:hover{
    background-color: #CC9787;

    -moz-transition: background-color 0.8s ease-in-out;
    -webkit-transition: background-color 0.8s ease-in-out;
    -ms-transition: background-color 0.8s ease-in-out;
  }

  #potato{
    font-color: #7a9ec7;
    color: #7a9ec7;
  }

  @keyframes fadein {
      from { opacity: 0; }
      to   { opacity: 1; }
  }

  /* Firefox < 16 */
  @-moz-keyframes fadein {
      from { opacity: 0; }
      to   { opacity: 1; }
  }

  /* Safari, Chrome and Opera > 12.1 */
  @-webkit-keyframes fadein {
      from { opacity: 0; }
      to   { opacity: 1; }
  }

  /* Internet Explorer */
  @-ms-keyframes fadein {
      from { opacity: 0; }
      to   { opacity: 1; }
  }

      h1 {
        font-family: inherit;
        font-size: "140pt";
      }

    </style>
  </head>
  <body>
<center id = 'main'>
    <h1 id = 'potato'> Welcome, Samaritan! </h1>
    <!--<h3 id="purrtato"> <%= link_to "CONTINUE", ques_path %><h3>-->
      <%= button_to "Continue", ques_path, :id => "purrtato", :type => "button", :method => :get %>
      <!-- change -->
    </center>
  </body>


</html>
